import { ActionList } from '@k-int/stripes-kint-components';

const NumberGeneratorConfig = () => {
  return (
    <div>
      Number generator settings go here
    </div>
  );
};

export default NumberGeneratorConfig;
